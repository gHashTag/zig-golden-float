module github.com/gashTag/zig-golden-float/go/goldenfloat

go 1.21

// This is a cgo wrapper package, so no external Go dependencies
// The C library (libgoldenfloat) is built separately via zig build shared
