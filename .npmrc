# GoldenFloat npm publishing config
# Use public registry (change to your private registry if needed)
registry=https://registry.npmjs.org/

# Org scope for packages
@golden-float:registry=https://registry.npmjs.org/

# Access token (set via env var: NPM_TOKEN)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
